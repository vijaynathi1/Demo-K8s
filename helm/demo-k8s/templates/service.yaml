apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-k8s.fullname" . }}
  labels:
    app: {{ include "demo-k8s.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "demo-k8s.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
      name: http
